{
	"name": "@hbxcontrols/sync-codes",
	"version": "1.0.10-dev.0",
	"description": "A set of tools for handling and validating HBX Controls device sync codes",
	"main": "src",
	"scripts": {
		"release:dev": "standard-version --prerelease dev --skip.tag && git push && npm publish --tag dev",
		"release:stable": "standard-version && git push --follow-tags && npm publish"
	},
	"author": "Chris Tohill <graphics@hbxcontrols.com>",
	"license": "MIT",
	"dependencies": {
		"standard-version": "^9.5.0"
	}
}
